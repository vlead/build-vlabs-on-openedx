import json
from pprint import pprint
from git import Repo
   
with open('gitrepos.json') as data_file:    
    data = json.load(data_file)
    for i in range(5):
        pprint (data[i]["html_url"].encode("utf-8"))

Repo.clone_from("https://github.com/vlabs-on-openedx/open-edx-computer-programming-iiith",
                          "/home/sravanthi/Music/automation-labs/computer-program")
